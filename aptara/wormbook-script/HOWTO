UPDATE: 30 OCTOBER 2013

1. Generating entity=URI key/value pairs

WormBook staff will periodically -- in accordance with WormBase releases --
generate updated lists of entity-to-URI mappings. These are stored in directories
corresponding to the current WormBase version: WS239, WS240, etc.

    ./WormbaseLinkGenerate.pl --links-from localhost

This command can ONLY be run on a server hosting the full WormBase database!


2. Marking up biological entities in docbook.xml with links to WormBase:

The command is:

   ./WormbaseLinkGenerater.pl --links-from WS239/ --xml-files docbook.xml

   Where --links-from points to a directory of entity=URI key value pairs.
         --xml-files is a file in docbook.xml.


Todd Harris (toddwharris@gmail.com)
30 Oct 2013